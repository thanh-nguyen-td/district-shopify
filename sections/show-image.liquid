{% style %}
    .h-img{
        height:200px
    }
{% endstyle %}
<div class="{%if section.settings.max_width != true%}page-width{% endif %}">
   {% if section.settings.image != blank %}
       <a href="{{section.settings.img_link}}">
            <img height="100%" src="{{section.settings.image | img_url: 'grande'}}"/>
       </a>
    {% else %}
        {{ 'image' | placeholder_svg_tag: 'h-img' }}
   {% endif %}
</div>

{% schema %}
{
    "name":"Show Image",
    "tag":"section",
   "settings": [
        {
            "type": "image_picker",
            "id": "image",
            "label": "select_img"
        },
        {
            "type": "url",
            "id": "img_link",
            "label": "link"
        },
        {
            "type": "checkbox",
            "id": "max_width",
            "default": true,
            "label": "max width"
        }
   ],
    "presets":[
        {
            "name":"Show Image" 
        }
    ]
}
{% endschema %}